version: '2.0'

services:
  api:
      build: api/.
      ports: 
        - "3000:3000"
      depends_on:
        - db
  db:
      image: "mongo"
      restart: always
      volumes:
        - ./data:/data/db